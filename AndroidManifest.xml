<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jxxy.tableshow"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="16" />

    <!-- 往sdcard中写入数据的权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 在sdcard中创建/删除文件的权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.jxxy.tableshow.activity.SplashActivity"
                        android:launchMode="singleTask"
            
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ProblemRecordActivity" 
                        android:launchMode="singleTask"
            >
        </activity>
        <activity android:name=".activity.InspectionBillsActivity" 
                        android:launchMode="singleTask"
            >
        </activity>
        <activity android:name=".activity.FileManager" 
                        android:launchMode="singleTask"
            >
        </activity>
        <activity android:name=".activity.ContractDetailsActivity" 
                        android:launchMode="singleTask"
            >
        </activity>
        <activity android:name=".activity.ProblemRecordListActivity" 
                        android:launchMode="singleTask"
            >
        </activity>
        <activity android:name=".activity.InspectionBillsListActivity" 
                        android:launchMode="singleTask"
            >
        </activity>
        <activity android:name=".activity.ProductRecordActivity"
                        android:launchMode="singleTask"
            ></activity>
        <activity android:name=".activity.ProductRecordDetailsActivity"
                        android:launchMode="singleTask"
            ></activity>
        <activity android:name=".activity.ProductRecordConclusionActivity"
                        android:launchMode="singleTask"
            ></activity>
        <activity android:name=".activity.ProductConclusionDetails"
                        android:launchMode="singleTask"
            ></activity>
        <activity android:name=".activity.ContractListActivity"></activity>
        <activity android:name=".activity.MainTopRightDialog" 
            android:theme="@style/MyDialogStyleTop"></activity>
        <activity android:name=".frgment.FramentMain"></activity>
        <activity android:name=".activity.EditCheckProblemActivity"></activity>
        <activity android:name=".activity.EditSuperviseExamineActivity"></activity>
        <activity android:name=".activity.EditSuperviseConditionActivity"></activity>
    </application>

</manifest>